package pizza_shop_system.order;
import pizza_shop_system.account.User;

import java.util.ArrayList;
import java.util.Scanner;

public class OrderCreate {
}
/*
 private static Scanner scanner = new Scanner(System.in);
public static Order createOrder(User user, ArrayList<MenuItem> menu) {

//selecting order type
String orderType = selectOrderType();

//Choose items
ArrayList<MenuItem> selectedItems = selectItems(menu);

//choose payment method
 System.out.print("Enter payment method (e.g., Card, Cash): ");
        String paymentMethod = scanner.nextLine();

//create order
Order order = new Order(paymentMethod);
        order.setOrderType(orderType);
        order.setOrderedItems(selectedItems);
        order.setStatus(OrderStatus.IN_PROGRESS);
        order.setAccount(user);

System.out.println("Order placed successfully!");
        order.generateAndDisplayReceipt(order, user.getName(), paymentMethod);

        return order;
    }

 private static String selectOrderType() {
        System.out.println("Select order type:");
        System.out.println("1. Pickup");
        System.out.println("2. Carry-Out");
        System.out.println("3. Delivery");
        int choice = Integer.parseInt(scanner.nextLine());






 */